# Ostia

> 隐私优先的 Nostr 即时通讯应用（端到端加密）

## 当前状态（2026-02-05）

- 版本：v1.0.0
- **已验证平台**：Windows、Android、iOS 均已完成验证测试
- **功能状态**：
  - Windows：核心功能完整，文本与图片消息正常
  - Android：功能完整，Windows ↔ Android 消息收发与离线同步正常
  - iOS：功能完整，已完成验证测试

[![License: GPLv3](https://img.shields.io/badge/License-GPLv3-blue.svg)](https://www.gnu.org/licenses/gpl-3.0)
[![Version: 1.0.0](https://img.shields.io/badge/Version-1.0.0-blue.svg)](https://github.com/outmanster/ostia)

Ostia 是一个基于 [Nostr 协议](https://nostr.com/) 的跨平台即时通讯应用，强调极简、隐私优先与去中心化。

## 功能特性

### 🔐 身份管理
- **安全密钥存储**：平台级 keyring（Windows Credential Manager / macOS Keychain / Linux Secret Service）
- **一键创建账户**：自动生成 Nostr 密钥对
- **导入私钥**：支持 nsec 格式导入
- **NIP-17 Gift Wrap**：隐藏元数据的高级隐私保护

### 📨 私密消息
- **NIP-17 协议**：现代化私聊模型
- **实时同步**：多设备即时投递
- **消息状态**：pending / sent / delivered / failed 可视化
- **失败重试**：发送失败可重试
- **会话隔离**：每个联系人独立聊天视图

### 👥 联系人系统
- **添加/移除联系人**：支持 npub / nprofile
- **联系人搜索**：快速过滤
- **资料解析**：自动拉取 Kind 0 资料
- **屏蔽**：阻止不受欢迎联系人

### 🔄 中继器管理
- **自定义中继器**：完全由用户配置
- **独占模式**：仅使用自定义中继器
- **实时状态**：连接状态可视化
- **NIP-65 支持**：可发布/查询用户中继器列表

### 💾 数据持久化
- **SQLite 存储**：本地快速存储
- **缓存管理**：自动缓存与过期
- **去重机制**：避免重复消息
- **离线同步**：重连后补齐消息

### 🎨 UI/UX
- **响应式设计**：桌面与移动端自适应
- **深浅主题**：支持主题切换
- **提示与通知**：可操作的状态反馈
- **连接指示**：实时中继器连接状态

## 安装

### Windows

从 [GitHub Releases](https://github.com/outmanster/ostia/releases) 下载最新安装包并运行。

### macOS

1. 在 [GitHub Releases](https://github.com/outmanster/ostia/releases) 下载 `.dmg`
2. 打开 DMG，将 Ostia 拖到 Applications
3. 首次运行可能需在 系统设置 → 隐私与安全 中允许

### Linux

#### AppImage
```bash
# 下载 AppImage
chmod +x Ostia-*.AppImage
./Ostia-*.AppImage
```

#### Debian/Ubuntu
```bash
sudo dpkg -i Ostia-*.deb
```

### 从源码构建

```bash
# 克隆仓库
git clone https://github.com/outmanster/ostia.git
cd ostia

# 安装依赖
pnpm install

# 开发
pnpm tauri dev

# 构建当前平台
pnpm tauri build
```

## 使用

### 快速开始

1. **启动 Ostia**：进入登录/注册界面
2. **创建账户** 或 **导入私钥**
3. **备份私钥**：务必保存好 nsec
4. **添加联系人**：点击 “+” 输入对方 npub
5. **开始聊天**：选择联系人开始对话

### 发送消息

1. 从侧边栏选择联系人
2. 在底部输入框输入消息
3. 回车发送，Shift+Enter 换行
4. 状态图标说明：
   - ⌛ Pending
   - ✓ Sent
   - ✓✓ Delivered
   - ✗ Failed（可重试）

### 中继器管理

1. 点击设置（⚙️）
2. 进入“中继器”页
3. 添加 WebSocket 地址（例如 `wss://relay.example.com`）
4. 至少配置一个中继器后才能收发消息

## 架构

### 技术栈

- **前端**：React 19 + TypeScript + Tailwind CSS + shadcn/ui
- **后端**：Rust + Tauri 2.0
- **协议**：Nostr（nostr-sdk v0.38）
- **存储**：SQLite + 平台安全存储

### 安全特性

- **私钥仅在 Rust 后端处理**
- `secrecy` 保护敏感内存
- 平台级安全存储（Keyring/Keychain/Keystore）
- 媒体加密采用 AES-256-GCM
- NIP-17 Gift Wrap 元数据保护

### 数据流

```
用户操作 → React 前端 → Tauri 命令 → Rust 后端 → Nostr 网络
                  ↑                              ↓
                  └──────────── 状态回传 ──────────┘
```

## 配置

### 默认中继器

应用**不内置默认中继器**，首次使用需手动添加。

### 自定义中继器

支持任意 NIP-01 中继器地址，例如：
- `wss://relay.snort.social`
- `wss://relay.highlighter.com`
- `wss://nostr.mom`

### 媒体上传（已实现）

1. 压缩为 WebP（最大 2048px）
2. AES-256-GCM 加密
3. 上传至配置的 Blossom 服务器
4. 返回带 `#key` 与 `#nonce` 的 URL 片段

注意：当前实现**只支持 Blossom**，未启用 NIP-96 自动发现。

## 开发

### 项目结构

```
src-tauri/src/
├── commands/       # Tauri 命令（account、messaging、contacts）
├── nostr/          # Nostr 协议服务
├── storage/        # 数据库与安全存储
└── utils/          # 错误类型与工具

src/
├── components/     # React 组件
├── hooks/          # 自定义 hooks
├── store/          # Zustand 状态管理
└── types/          # TypeScript 类型
```

### 常用命令

```bash
# 安装依赖
pnpm install

# 开发服务
pnpm tauri dev

# 构建当前平台
pnpm tauri build

# 构建指定平台
pnpm tauri build --target x86_64-pc-windows-msvc

# 运行 Rust 测试
cargo test

# 运行 TypeScript 类型检查
pnpm tsc
```

## 故障排查

### “无法连接中继器”
- 确认网络连接
- 检查中继器地址是否正确
- 尝试添加其他中继器
- 检查中继器是否宕机：[Nostr.watch](https://nostr.watch)

### “未找到私钥”
- 确认已保存私钥
- 检查是否导入了正确的 nsec
- 重新导入一次

### “消息无法发送”
- 确认已配置中继器
- 检查系统时间是否准确
- 尝试更换中继器

### “图片发送失败”
- 确认已配置媒体服务器
- 检查媒体服务器是否可达
- 确认 URL 以 http/https 开头

### “构建失败”
```bash
# 清理并重新构建
cargo clean
pnpm install
pnpm tauri build
```

## 参与贡献

欢迎贡献代码！请阅读 [贡献指南](CONTRIBUTING.md)。

### 开发流程

1. Fork 仓库
2. 创建分支（`git checkout -b feature/amazing-feature`）
3. 提交修改（`git commit -m 'feat: add amazing feature'`）
4. 推送分支（`git push origin feature/amazing-feature`）
5. 创建 Pull Request

## 许可证

 проекта遵循 MIT 许可证，详见 [LICENSE](LICENSE)。

## 致谢

- [Nostr Protocol](https://nostr.com/) - 项目基础协议
- [Tauri](https://tauri.app/) - 跨平台框架
- [nostr-sdk](https://github.com/nostr-rs/nostr) - Nostr 协议实现
- [shadcn/ui](https://ui.shadcn.com/) - UI 组件库

## 支持

- **GitHub Issues**：[反馈问题或需求](https://github.com/outmanster/ostia/issues)
- **Nostr**：npub 后续公布
- **Discussions**：[GitHub Discussions](https://github.com/outmanster/ostia/discussions)
